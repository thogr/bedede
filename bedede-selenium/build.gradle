apply from: rootProject.file("publish.gradle")

dependencies {
    compile libraries.selenium
    compile project(':bedede-core')

    testCompile libraries.selenium

    //examplesCompile libraries.selenium
    //examplesCompile project(':bedede-core')
}

